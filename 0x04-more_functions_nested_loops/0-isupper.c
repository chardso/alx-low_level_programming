#include "main.h"
/**
 * _isupper - returns 1 if upper case else 0
 * Return: 0
 * @c: is to passed into the function
 * Checks if character is upper case
*/
int _isupper(int c)
{
		if isupper(c)
		{
				return (1);
		}
		else
		{
			return (0);
		}
}

