#include "main.h"

/**
 * _isupper - Checks for an uppercase letter
 * @c: int to be checked
 * Return: 1 if c is uppercase, otherwise return 0
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 90)
{
return (1);
}
else
{
return (0);
}
}

