#include "main.h"
/**
 * _strlen - length of a string
 * @s: pointer to a char
 * Return: Always 0
 */
int _strlen(char *s)
{
int counter;

for (counter = 0; s[counter] != 0; counter++)
{
}

return (counter);
}

