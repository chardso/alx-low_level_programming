#include "main.h"
#include <ctype.h>
/**
 * _islower - Checks for lowercase char
 * @c: character to check if islower
 * Return: Always 0
 */
int _islower(int c)
{
if (islower(c))
{
return (1);
}
else
{
return (0);
}
}

