#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * _puts - prints a string followed by a new line to stdout
 * @str: pointer to a char
 * Return: Always 0
 */
void _puts(char *str)
{
puts(str);
}

